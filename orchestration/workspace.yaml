# =============================================================================
# Dagster Workspace Configuration
# Tells Dagster where to find our code (Definitions)
# =============================================================================

load:
  - python_file: weather_pipeline/__init__.py
